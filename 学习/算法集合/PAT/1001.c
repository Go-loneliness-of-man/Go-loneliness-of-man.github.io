#include<stdio.h>
//卡拉兹猜想，任何自然数，若是偶数砍去一半，若是奇数将其 （3n + 1） 砍去一半，循环下去即可使 n 最终等于 1。
//题目要求：给定任意不超过 10000 的数，数出一共需要砍几下才能得到 n = 1？
int main()
{
	int a, i;
	scanf("%d", &a);
	for (i = 0; a != 1; i++)
	{
		if (a % 2 == 0)	a /= 2;
		else a = (3 * a + 1) / 2;
	}
	printf("%d", i);
	return 0;
}